{
  "$schema": "../schema/workgrove.schema.json",
  "version": 1,
  "stride": 10,
  "setup": { "argv": ["uv", "sync"] },
  "start": { "argv": ["uv", "run", "python", "-m", "app"] },
  "apps": {
    "api": { "basePort": 8000 }
  },
  "env": {
    "PORT": "{apps.api.port}"
  }
}
